name: 🔄 Keep Capri Store Alive

on:
  schedule:
    # Ejecutar cada 10 minutos (evita que Render duerma y permite reconexión WhatsApp)
    - cron: '*/10 * * * *'
  
  # Permitir ejecución manual desde GitHub
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 🔄 Keep-Alive Health Check (triggers auto-reconnect)
        run: |
          echo "🔄 Keep-alive con reconexión automática de WhatsApp..."
          response=$(curl -s -w "HTTPSTATUS:%{http_code}" -H "User-Agent: GitHub-Actions-Keep-Alive" https://capri-store.onrender.com/health)
          http_code=$(echo $response | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')
          body=$(echo $response | sed -e 's/HTTPSTATUS:.*//g')
          
          echo "📊 Status Code: $http_code"
          
          if [ $http_code -eq 200 ]; then
            echo "✅ Servicio activo (WhatsApp intentará reconexión automática si es necesario)"
            
            # Extraer estado de WhatsApp
            if command -v jq &> /dev/null; then
              whatsapp_ready=$(echo "$body" | jq -r '.whatsapp_ready // false')
              echo "📱 WhatsApp: $whatsapp_ready"
            fi
          else
            echo "❌ Servicio no responde"
            exit 1
          fi

      - name: 📊 Memory Check
        run: |
          echo "📊 Verificando uso de memoria..."
          response=$(curl -s -w "HTTPSTATUS:%{http_code}" https://capri-store.onrender.com/memory-status)
          http_code=$(echo $response | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')
          body=$(echo $response | sed -e 's/HTTPSTATUS:.*//g')
          
          echo "📊 Memory Status Code: $http_code"
          echo "📄 Memory Response: $body"
          
          if [ $http_code -eq 200 ]; then
            echo "✅ Información de memoria obtenida"
            
            # Extraer porcentaje de uso si está disponible
            if command -v jq &> /dev/null; then
              usage_percent=$(echo "$body" | jq -r '.render_info.usage_percent // "N/A"')
              echo "📊 Uso de memoria: $usage_percent%"
              
              if [ "$usage_percent" != "N/A" ] && [ "$usage_percent" -gt 85 ]; then
                echo "⚠️ USO DE MEMORIA ALTO: $usage_percent%"
              fi
            fi
          else
            echo "⚠️ No se pudo obtener información de memoria"
          fi

      - name: 🎯 Summary
        run: |
          echo ""
          echo "🎯 ===== RESUMEN DE KEEP-ALIVE ====="
          echo "📅 Fecha: $(date)"
          echo "🔄 Servicio mantenido activo exitosamente"
          echo "📱 Auto-reconexión WhatsApp: cada 10 min si desconectado"
          echo "⏰ Próxima ejecución: en 10 minutos"
          echo "🌐 URL: https://capri-store.onrender.com"
          echo "=================================="
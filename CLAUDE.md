# Capri — Migración a panel admin web

Este repo es el backend/frontend de la tienda online de Capri (Node/Express + HTML/Bootstrap plano, sin framework de frontend). Está en producción: procesa pagos reales de MercadoPago. **Cualquier cambio debe evitar romper el checkout existente.**

Objetivo del proyecto: convertir esto en el producto integrado de gestión del negocio (hoy vive en una app de escritorio Java separada) + venta online, todo en un mismo backend web. Ver plan de fases más abajo — se implementa de a una fase por vez, en orden, sin saltar pasos.

## Arquitectura actual

- **`js/server.js`**: bootstrap de Express (env, CORS, middlewares, arranque). Rutas y lógica de negocio viven en módulos separados (ver debajo). Punto de entrada real: `start-optimized.js` en la raíz (ver `package.json` → `scripts.start`).
- **`js/db.js`**: pool de Postgres (`pg.Pool`), `initializeDatabase`, retry de queries, utilidades de esquema/tabla y de payment id.
- **`js/services/mercadopago.js`**: cliente de MercadoPago (`MercadoPagoConfig`, `Preference`, `Payment`).
- **`js/services/whatsapp.js`**: lógica de notificaciones de compra (envío, reintentos, normalización de teléfono/plantillas). Usa `js/whatsapp-api-service.js` (API oficial de Meta) para el envío real.
- **`js/routes/store.js`**: todas las rutas de catálogo/checkout/webhook de MercadoPago/health/debug que antes vivían inline en `server.js`.
- **`js/routes/admin.js`**: scaffold vacío, ahí van las rutas del panel admin a partir de Fase 1.
- **Postgres compartido**: este backend se conecta con `pg.Pool` a la **misma base de datos** que usa la app de escritorio Java (ver sección siguiente). Tabla clave: `productos` (cada fila = 1 unidad física: `id_articulo`, `prenda`, `color`, `talle`, `estado`, `publicado_en_web`). El checkout crea pedidos vía stored procedure `sp_crear_pedido_web`. **No cambiar el esquema de esta tabla sin coordinarlo con el lado Java** (rompe `Controlador_Pestanias.java`).
- **GCS bucket `imagenes-web-capri`**: imágenes + archivos `.txt` de catálogo (`productos.json` como índice). Ver detalle en memoria de proyecto si hace falta (`Novedades/{id}-{Nombre}-{Color}/`, txt de 4 líneas: Nombre/Descripción/Precio/Detalle).
- **MercadoPago**: `mercadopago` SDK, checkout + webhook con validación de stock.
- **WhatsApp**: `js/whatsapp-api-service.js` (API oficial de Meta, único medio activo). El enfoque anterior con `whatsapp-web.js` (QR) — `js/whatsapp-service.js`, `js/postgres-auth-strategy.js` — se eliminó en Fase 0 por no tener consumidor real; si aparece una referencia a esos archivos en una rama vieja, es código muerto.
- **`js/services/auth.js`**: auth del panel admin (Fase 1) — usuario fijo + `bcryptjs` + JWT en cookie httpOnly. Ver `js/routes/admin.js` para el middleware que protege `/admin/*`.
- **Hosting**: Render (`render.yaml`), memoria acotada (`--max-old-space-size=450/512` en los scripts de npm). Evitar features que carguen mucha memoria en el proceso principal.
- **Sin tests automatizados** (`npm test` es un stub). Verificar cambios manualmente contra `TEST-PLAN.md` o levantando el server local y probando el flujo a mano.

## El otro lado: app de escritorio Java (fuente de la lógica de negocio a migrar)

Repo separado en esta misma máquina: `C:\Users\guillermo.gavotti\IdeaProjects\SistemaCapri\SistemaCapri\`

Al implementar cada módulo del admin web, **leer primero el equivalente Java** para no perder reglas de negocio (validaciones, cálculos, estados) que hoy solo existen ahí:

| Módulo a migrar | Archivo Java de referencia |
|---|---|
| Controlador principal / todas las pestañas (~3300 líneas, leer en trozos) | `src\main\java\org\sistema\Controlador_Pestanias.java` |
| Modelo de artículo de venta | `src\main\java\org\sistema\ModuloVentas\RegistroArticuloVentas.java` |
| Carga de lotes / productos | `src\main\java\org\sistema\ModuloCargaArchivo\RegistroLote.java`, `RegistroProducto.java` |
| Clientes | `src\main\java\org\sistema\ModuloCargaArchivo\RegistroCliente.java` |
| Pagos | `src\main\java\org\sistema\ModuloCargaArchivo\RegistroPago.java` |
| Gastos extra | `src\main\java\org\sistema\ModuloCargaArchivo\RegistroGastoExtra.java` |
| Cash flow | `src\main\java\org\sistema\ModuloCashFlow\RegistroTransaccionCF.java` |
| Conexión a DB (para ver esquema/convenciones) | `src\main\java\org\sistema\config\DatabaseConfig.java` |
| Config de Google Cloud | `src\main\java\org\sistema\GoogleCloudConfig.java` |

`Controlador_Pestanias.java` es grande — leerlo en fragmentos (~200 líneas) buscando el método relevante en vez de cargarlo entero.

## Cómo trabajar en este proyecto

- **Una fase por sesión.** No adelantar fases futuras dentro de la misma tarea aunque parezca fácil — cada una se revisa y confirma antes de seguir.
- Al terminar una fase, **marcar el checklist de abajo** (`[x]`) y dejar una línea breve de qué se hizo, para que la próxima sesión arranque sin tener que releer todo el diff.
- No tocar rutas de checkout/webhook existentes salvo que la fase lo requiera explícitamente.
- Nuevas rutas de admin van en `routes/admin.js` (crear en Fase 0), no inline en `server.js`.
- Cualquier endpoint de escritura sobre `productos` debe replicar las validaciones que hace el Java (estado, publicado_en_web, etc.), no solo el statement SQL.

## Plan de fases

- [x] **Fase 0 — Modularizar `server.js`.** Extraído sin cambiar comportamiento a `js/db.js` (pool Postgres, retry, payment id utils), `js/services/mercadopago.js`, `js/services/whatsapp.js` (notificaciones), `js/routes/store.js` (catálogo/checkout/webhook) y `js/routes/admin.js` (scaffold vacío para Fase 1). `js/server.js` quedó como bootstrap de Express. Se confirmó por grep que `whatsapp-service.js`, `postgres-auth-strategy.js*`, `requireMobileApiKey` y las rutas `/api/whatsapp-status`, `/api/notificaciones-pendientes` y `/limpiar-sesiones-whatsapp` (esta última ya estaba rota, llamaba a una función inexistente) no tenían ningún consumidor real — se borraron junto con las dependencias `whatsapp-web.js`/`qrcode-terminal` en `package.json`. Verificado localmente: server levanta, sirve estáticos, `/health`, `/debug`, `/contact-info`, `/webhook`, `/crear-preferencia` responden igual que antes. Nota: se preservaron dos bugs preexistentes tal cual estaban (no se tocaron, fuera de alcance de esta fase): el bloque `whatsappService` no definido en `/memory-status` y `/cleanup-memory`, y las rutas duplicadas `/memory-status`/`/debug` (la segunda definición de cada una queda inalcanzable, igual que en el original).
- [x] **Fase 1 — Auth de admin.** Login con usuario fijo (`ADMIN_USERNAME`) + password hasheado con `bcryptjs` (`ADMIN_PASSWORD_HASH`, generar con `node scripts/hash-password.js "password"`) y JWT (`jsonwebtoken`) en cookie httpOnly (`js/services/auth.js`). `js/routes/admin.js`: `POST /admin/login`, `POST /admin/logout` públicas; a partir de ahí `router.use(...)` exige JWT válido para cualquier ruta futura de `/admin/*` (incluye `GET /admin/me` para probar la sesión). Freno de fuerza bruta simple en memoria (5 intentos fallidos → bloqueo de 15 min por IP). Páginas `admin-login.html` y `admin.html` (placeholder) en la raíz. No se reusó la API-key legacy (ya eliminada en Fase 0). Verificado localmente: sin cookie → 401, password incorrecta → 401, login correcto → set-cookie + 200, `/admin/me` con cookie → 200, logout invalida la cookie, 6to intento fallido → 429, y el resto del sitio (checkout, health) sigue intacto. `ADMIN_USERNAME`, `ADMIN_PASSWORD_HASH` y `JWT_SECRET` ya están configuradas en Render (confirmado 2026-07-14). El `.env` local tiene credenciales de prueba (`admin` / `TestAdmin123!`) solo para desarrollo.
- [x] **Fase 2 — Artículos Web.** `js/services/gcs.js` (cliente de Google Cloud Storage: sube/lee/borra blobs del bucket `imagenes-web-capri`, upsert de `productos.json`, parseo/generación del `.txt` de catálogo en el formato nuevo de 4 líneas — sin talle — con fallback de lectura al formato viejo de 5 líneas). `js/routes/admin/articulos-web.js` (montado en `/admin/articulos-web`, protegido por el middleware de auth de Fase 1): `GET /productos` (equivalente a `obtenerProductosDesdeBD`, para poblar la tabla de selección), `GET /:idArticulo` (equivalente a `leerDatosTxtDesdeCloud`, para precargar el form de edición), `POST /generar` (equivalente a `generarYSubirArticulo` — multipart con 1 imagen; a diferencia del desktop que sube varios colores en un solo click, acá **una llamada = un color/carpeta**, con la misma validación de "misma prenda" reforzada a "misma prenda y mismo color" por artículo seleccionado — simplificación de UI, no de reglas de negocio: cada color sigue generando su propia carpeta/entrada igual que en Java), `PUT /:idArticulo` (equivalente a `modificarArticuloEnWeb`, imagen nueva opcional), `POST /ajuste-porcentual` (equivalente a `aplicarAjustePorcentualEnWeb`, solo afecta artículos con `publicado_en_web = 'True'`, reporta `omitidos` con motivo en vez de tragárselos en silencio). Todas las rutas exigen `estado = 'Disponible'` igual que el filtro SQL del desktop. Pantalla `admin.html` reescrita con las 3 acciones + tabla de productos con checkboxes y filtro por prenda.
  Se sumó además `DELETE /:idArticulo` (equivalente a `quitarDeLaWeb`: borra la carpeta completa de GCS, la entrada de `productos.json` y desmarca `publicado_en_web` — igual que el desktop, solo desmarca el `id_articulo` puntual aunque la carpeta borrada sea compartida por otros talles del mismo color; limitación preexistente del Java, no introducida acá). Botón "Quitar" agregado a la tabla de `admin.html` para filas publicadas.
  **Verificado localmente** (ya con `DATABASE_URL` y `GOOGLE_APPLICATION_CREDENTIALS` reales cargados en el `.env` local, usando las credenciales que el usuario copió a la raíz — ver `.gitignore`): `GET /productos` responde con datos reales de la BD compartida con el desktop.
  `GOOGLE_APPLICATION_CREDENTIALS_JSON` ya está configurada en Render y el bug 500 de `POST /generar` en producción **ya no ocurre** (confirmado 2026-07-14). Dependencias nuevas: `@google-cloud/storage`, `multer` (agregadas a `package.json`).
- [x] **Fase 3 — Stock / carga de lotes.** `js/routes/admin/stock.js` (montado en `/admin/stock`, protegido por el middleware de auth de Fase 1): `GET /` lista todo el stock (sin filtrar por estado, a diferencia de Artículos Web) usando el mismo stored procedure que la pestaña Ventas/Stock del desktop (`sp_consultar_listado_ventas`); filtrado por ID/prenda/estado/categoría queda del lado del cliente (mismo criterio "contains" case-insensitive que `filtrarVentas()` en Java). `POST /lotes` es el equivalente web de la pestaña "Carga Manual" del desktop (`insertarLoteYProductos` → `CALL sp_inserta_lote_y_productos(costo, in_local, inversor, productos_json)`): recibe `{ inversor, observacion, productos: [{prenda, categoria, color, talle, precio_compra, precio_venta_transferencia}, ...] }`, valida campos obligatorios y que precio_venta ≥ precio_compra (igual que `validarCamposProducto`), calcula `precio_venta_efectivo` como 90% del de transferencia (mismo descuento por efectivo que `agregarProducto`), suma `precio_compra` de todos los productos para el costo del lote, y todos los productos entran como `estado = 'Disponible'`. Nota preservada del Java: el parámetro de la SP se llama `in_local` pero el campo de UI es "Observación" — inconsistencia preexistente, no se tocó. No se migró la carga masiva por Excel (`cargarExcelABBDD`, hojas Lotes/Productos/Pagos/Gastos Extra/Clientes) — cruza con módulos de Pagos/Gastos Extra/Clientes que todavía no están migrados (Fases 4/5); si hace falta más adelante, evaluar aparte.
  `admin.html` ahora tiene tabs ("Artículos Web" / "Stock"): la pestaña Stock trae la tabla de stock actual con filtros y una sección de carga manual de lote (agregar productos a una lista en memoria del lado del cliente, con precio venta efectivo auto-calculado en la tabla, y "Finalizar carga" que manda todo junto a `POST /admin/stock/lotes`).
  **Aclaración importante sobre "talles y colores":** se investigó un diff sin commitear en el repo Java (`Controlador_Pestanias.java`) sobre agrupar en una sola tarjeta web varios `id_articulo` de la misma prenda con distintos color/talle (una imagen por color, sin talle en el .txt) — eso es exactamente lo que ya migramos en Fase 2 (Artículos Web), no afecta el esquema de `productos` ni esta fase. Se confirmó contra la BD real que `productos` sigue siendo 1 fila = 1 unidad física con su propio `talle`/`color` (sin cambios de esquema); no hace falta lógica de agrupación en la carga de stock, cada producto se inserta como su propia fila igual que hoy.
  **Verificado localmente** contra la BD real: `GET /admin/stock` devuelve el listado completo; `POST /admin/stock/lotes` con un producto de prueba insertó correctamente en `productos`/`lotes` (precio_venta_efectivo calculado bien, 2000→1800) y se limpiaron los datos de prueba después. Validaciones probadas: observación/inversor obligatorios, al menos un producto, precio_venta < precio_compra rechazado. Confirmado que `/admin/articulos-web/*`, `/health` y el guard de auth (401 sin cookie) siguen intactos.
- [x] **Fase 4 — Ventas presenciales.** `js/routes/admin/ventas.js` (montado en `/admin/ventas`, protegido por el middleware de auth de Fase 1): `POST /confirmar` (equivalente a `confirmarCompra` → `CALL sp_procesar_venta(monto_total, nombre_cliente, metodo_pago, lista_articulos)`) recibe `{ ids, metodoPago, nombreCliente }`, valida que `metodoPago` sea `'Efectivo'` o `'Transferencia'`, que todos los `ids` existan y estén `estado = 'Disponible'` (misma condición que en el desktop habilita el botón "Agregar al carrito" en vez de "Devolución" — acá se re-valida server-side por si el carrito del cliente quedó desactualizado) y **recalcula el total en el servidor** a partir de `precio_venta_efectivo`/`precio_venta_transferencia` según el método elegido, en vez de confiar en un total mandado por el cliente. `nombreCliente` vacío cae a `'No proporcionado'` (igual que el desktop); los campos Instagram/correo que el desktop pedía en el formulario nunca se persistían en `sp_procesar_venta` (solo viajaba `nombreCliente`), así que no se migraron — no son datos reales, eran campos muertos en la UI de escritorio. `POST /devolver/:idArticulo` (equivalente a `devolverArticulo` → `CALL sp_devolver_articulo(id_articulo)`, que borra el pago asociado y vuelve a dejar el producto en `Disponible`); id inexistente → 404 con el mensaje que levanta la propia stored procedure.
  El listado de artículos para armar el carrito reutiliza `GET /admin/stock` (Fase 3, mismo `sp_consultar_listado_ventas` que usaba `tblArticulosStock` en la pestaña Ventas del desktop) — no se duplicó una ruta de listado nueva.
  `admin.html` suma una tercera pestaña "Ventas": tabla de artículos con botón "Agregar" (si `Disponible`) o "Devolución" (si no), y una sección de carrito (agregar/quitar en memoria del lado del cliente, radio de método de pago Efectivo/Transferencia con recálculo de total en vivo, campo de cliente opcional, "Confirmar venta"/"Cancelar").
  **Verificado localmente** contra la BD real: login + `/admin/me` OK; vendí el artículo de prueba #4 vía `POST /admin/ventas/confirmar` (pasó a `Sin Stock`, total calculado bien); reintentar venderlo de nuevo devolvió 400 ("ya no disponible"); método de pago inválido devolvió 400; `POST /admin/ventas/devolver/4` lo volvió a `Disponible` y borró el pago asociado (confirmado que no quedó fila huérfana en `pagos`); devolver un id inexistente devolvió 404; sin cookie de sesión devolvió 401. Confirmado que `/admin/articulos-web/*`, `/admin/stock` y `/health` siguen intactos.
- [x] **Fase 5 — Cash flow.** `js/routes/admin/cashflow.js` (montado en `/admin/cashflow`, protegido por el middleware de auth de Fase 1): `GET /?fechaInicio=YYYY-MM-DD&fechaFin=YYYY-MM-DD` (equivalente a `ejecutarConsulta` → `SELECT * FROM sp_analizar_cashflow(fecha_inicio, fecha_fin, 1)`, la opción 1 es la única que usa el desktop). La pestaña Cash Flow del desktop es de solo lectura — no hay alta manual de transacciones, el SP arma todo agregando `pagos` (ingresos), `gastos_extra` y `lotes` (gastos) del rango; no se migró nada de alta de gastos/pagos porque esos módulos no están migrados todavía (dependen de Fases futuras / carga Excel, fuera de alcance). El SP devuelve las transacciones con saldo acumulado más una fila final "ANÁLISIS" (con `fecha_transaccion IS NULL`) que trae un array de 10 métricas; esa fila se separa server-side (nunca se expone como transacción) y de ahí se devuelven las 8 métricas que el desktop realmente mostraba en pantalla (cantidad de ventas/gastos, ingresos/gastos/saldo totales, mayor ingreso, día de mayor ingreso/gasto) — se omiten a propósito `ticket_promedio` y `mayor_gasto` (índices 5 y 6 del array) porque el desktop tampoco los mostraba en ningún label.
  Valida fechas obligatorias y `fechaInicio <= fechaFin` (mismo mensaje "Intervalo de fechas incorrecto" que la alerta del desktop).
  **Corrección de un bug preexistente, no una decisión de negocio nueva:** el desktop convierte los timestamps epoch de "día de mayor ingreso/gasto" a fecha usando la zona horaria del sistema (`ZoneId.systemDefault()`), pero el SP los genera como epoch de medianoche UTC — en una máquina con huso horario negativo (ej. Argentina, UTC-3) eso corre la fecha mostrada un día hacia atrás. Además, si no hay gastos en el rango (`mayor_gasto` queda en 0 sin coincidencias), el array trae `NULL` en esa posición y el desktop lo desreferencia directo (`finalMetrics[9].longValue()`), lo que revienta con NPE. Acá se convierte usando UTC (sin corrimiento de día) y se maneja el caso `NULL` devolviendo `null` en vez de crashear.
  `admin.html` suma una cuarta pestaña "Cash Flow": selector de fecha inicial/final, botón "Consultar" (deshabilitado hasta completar ambas fechas) / "Limpiar", tira de métricas y tabla de transacciones (Fecha/Debe/Haber/Saldo/Observación).
  **Verificado localmente** contra la BD real: `GET /admin/cashflow` con rango amplio (2020–2026) devolvió 44 transacciones reales y métricas coherentes (6 ventas, gastos de lotes reales, saldo negativo esperado); falta de fechas → 400; `fechaInicio > fechaFin` → 400 "Intervalo de fechas incorrecto"; rango sin datos → `transacciones: []` y métricas en 0/null sin error; sin cookie → 401. Confirmado que `/admin/stock`, `/health` y el resto de rutas admin siguen intactos.
  **Extensión pedida por el usuario, mismo día:** detalle de artículos en Cash Flow. El SP solo devuelve `"Pago - {nombre_cliente}"` / `"Lote - {local}"` sin decir qué artículos componían esa venta/lote, y una devolución (`sp_devolver_articulo`) borra el pago asociado sin dejar ningún rastro de qué se devolvió ni por qué monto — nada de esto se podía resolver sin tocar SPs compartidas con el desktop, así que se resolvió enteramente del lado web:
  - `js/db.js`: nueva tabla propia de la web `devoluciones_web` (`DEVOLUCIONES_TABLE`), creada con `CREATE TABLE IF NOT EXISTS` al arrancar (`initializeDatabase`). No la toca ni la conoce el desktop; solo guarda un snapshot de cada devolución (artículo, monto, método de pago, cliente, fecha).
  - `js/routes/admin/ventas.js` (`POST /devolver/:idArticulo`): antes de llamar a la SP (que borra el pago), lee el artículo + su pago asociado; si la SP tiene éxito, inserta una fila en `devoluciones_web` con el monto recalculado igual que en `/confirmar` (precio efectivo o transferencia según el método de pago original, no `pagos.monto`, porque ese monto es el total de todo el carrito y puede incluir otros artículos). Si el artículo no tenía pago asociado (ej. ya estaba Disponible), no inserta nada — nada que loguear.
  - `js/routes/admin/cashflow.js`: además de la consulta al SP, hace consultas propias a `pagos`/`lotes` + `productos` (por `id_pago`/`id_lote`) para armar "prenda color talle" y enriquecer las observaciones `"Pago - ..."` / `"Lote - ..."` con esos artículos entre paréntesis. El emparejamiento con las filas que devuelve el SP (que no trae `id_pago`/`id_lote`) es por clave compuesta fecha+monto+texto libre — un enriquecimiento visual, no cambia debe/haber/saldo, y en el peor caso (dos transacciones idénticas el mismo día) podría mezclar el detalle entre sí. También agrega filas informativas `"Devolución - {artículo} ($monto)"` leídas de `devoluciones_web`, con `debe`/`haber`/`saldo` en `null` a propósito: el impacto real en el saldo ya está reflejado por la desaparición del pago original de la tabla `pagos`, así que sumar/restar de nuevo duplicaría el efecto — son puramente informativas.
  - `admin.html`: la celda de Saldo muestra "—" cuando la fila no trae saldo (las de devolución).
  **Verificado localmente** con un ciclo completo real: lote de prueba → venta (Transferencia, $200) → `GET /admin/cashflow` mostró `"Pago - ... (TEST-CF-PRODUCTO Rojo M)"` y `"Lote - ... (TEST-CF-PRODUCTO Rojo M)"` → devolución → la línea de Pago desapareció (comportamiento ya existente de la SP) y apareció `"Devolución - TEST-CF-PRODUCTO Rojo M ($200)"`; confirmado que devolver dos veces el mismo artículo (segunda vez sin pago asociado) no duplica la fila en `devoluciones_web`. Datos de prueba borrados al terminar.
- [x] **Fase 5.5 — Tarjeta por producto + varias fotos por color** (pedido del usuario, 2026-08-07). Revisión de coherencia del alta de productos que encontró un desajuste real: **la tarjeta del catálogo estaba indexada por `id_articulo` y no por producto**, así que publicar dos unidades de la misma prenda generaba dos tarjetas idénticas (pasaba en producción con "Body trikini" 74/75, "vestido valen" 147/148 y "Vestido Morley verde largo" 171/172), mientras `detalle.html` ya unía esas variantes por `prenda` vía `/variantes-producto/:id`. El catálogo partía lo que el detalle unía. Además `cargarImagenesPorColor()` de `detalle.js` (cambiar la foto al elegir color) **estaba inerte en producción**: filtraba por `p.prenda`/`p.color` y ninguna de las 45 entradas publicadas tenía esos campos.
  - **Formato de `productos.json`**: pasó a estar indexado por prenda, con `colores: [{ color, ids, imagenes: [] }]` (varias fotos por color). El formato nuevo es un **superconjunto** del viejo: se conservan `categoria`/`carpeta`/`imagen`/`txt`, y `carpeta` **mantiene el prefijo `{id}-`** a propósito porque `main.js`/`detalle.js` derivan el `id_articulo` parseando `/(\d+)-[^/]+/` de la URL (marcado de "sin stock" y resolución del detalle). Helpers en `js/services/gcs.js`: `slugify`, `normalizeProductoEntry` (lee ambos formatos), `buildProductoEntry`, `findEntryIndexByArticuloId`, `findEntryIndexByProducto`.
  - **`js/routes/admin/articulos-web.js`**: `POST /generar` pasó de "una llamada = un color" a **una llamada = una prenda con todos sus colores**, cada uno con N fotos. Los archivos llegan como `img_<indice>` + un campo `ordenColores` (JSON) que el servidor mapea a nombres de color; así el criterio de slug para el nombre del blob vive **solo del lado servidor** y no hay riesgo de que cliente y servidor calculen slugs distintos (se aceptan también `img_<slug>` y el `imagen` suelto por compatibilidad). Republicar solo algunos colores **no borra** los demás (publicación incremental); subir fotos de un color **reemplaza** las de ese color borrando las anteriores de GCS. `PUT` idem, y ahora el precio se propaga a **todas** las unidades de la tarjeta (antes tocaba un solo id, lo que con la tarjeta agrupada dejaría precios distintos en una misma tarjeta). `DELETE` desmarca `publicado_en_web` de **todas** las unidades que cubría la tarjeta — esto **corrige la limitación heredada del desktop** anotada en Fase 2 (borraba la carpeta compartida pero desmarcaba un solo id, dejando unidades marcadas como publicadas sin imagen).
  - **`js/catalogo.js`** (nuevo, compartido por `index.html` y `detalle.html`): `normalizarEntrada` / `agruparPorProducto` / `imagenesDeColor`. Agrupa las entradas por producto para que **una prenda = una tarjeta** incluso con datos en formato viejo (fusiona los duplicados sin esperar la migración). `imagenesDeColor` devuelve las fotos del color pedido y, si ese color no tiene mapeo (formato viejo), cae a mostrar todas.
  - **`js/main.js`**: agrupa por producto al cargar el catálogo; `aplicarEstadoStock` ahora marca "Sin stock" solo si están agotadas **todas** las unidades de la tarjeta (antes miraba un único id derivado de la URL).
  - **`js/detalle.js` + `detalle.html`**: galería con tira de miniaturas (`#galeria-thumbs`, oculta si hay una sola foto); al elegir un color se cambia **toda la galería** a las fotos de ese color. `cargarImagenesPorColor` se reemplazó por `cargarEntradaCatalogo`, que busca la entrada **por `id_articulo`** (exacto) y usa el nombre de prenda solo como fallback, porque los nombres difieren en espacios/mayúsculas entre BD y carpeta (`'Bandò '`, `'Vestido Morley verde largo '`).
  - **`admin.html`**: "Generar y subir" permite seleccionar varios colores a la vez y muestra **un input de archivos múltiple por color** de la selección; "Modificar" muestra un input por color existente y aclara que dejar uno vacío no lo toca.
  - **`scripts/migrar-productos-json.js`** (nuevo): reconstruye `productos.json` agrupando por prenda, resolviendo el **nombre del color desde la BD** por el id de la carpeta (el color no está en el JSON viejo). **Dry-run por defecto**, `--apply` para escribir y deja backup en el bucket (`productos.json.bak-<ts>`). **No se corrió con `--apply`** — queda a criterio del usuario.
  - **Verificado**: 22 asserts sobre las rutas con GCS stubbeado en memoria (no se tocó el bucket real): publicar 2 colores con 3+2 fotos → 1 sola tarjeta, blobs `vison-1.png`/`negro-1.png`, republicar un color conserva el otro, falta de foto → 400 nombrando el color, `DELETE` desmarca las 2 unidades y borra la carpeta; `publicado_en_web`/precio de la BD real **restaurados** al terminar. 19 asserts de `js/catalogo.js` contra el `productos.json` real: 45 entradas → **42 tarjetas** (mismo resultado que el script de migración), los 3 duplicados fusionados, compatibilidad de `categoria`/`txt`/`imagen`/`ids` con el render existente, y mezcla de formatos viejo+nuevo del mismo producto en una sola tarjeta. Smoke test del server: `catalogo.js` servido 200, ambas páginas lo incluyen, `/variantes-producto/74` devuelve `Negro`/`Visón` (contrato intacto), `GET /admin/articulos-web/74` funciona sobre una entrada en formato viejo, `/health` 200 y guard de auth 401 sin cookie.
  - **Dato sucio detectado, no corregido todavía:** los talles en BD no están normalizados (`'unico'` ×181, `'M'` ×28, pero también `'u'`, `'m'` minúscula, `'L (CHICA)'`) y `/variantes-producto` agrupa por string exacto, así que hoy genera talles separados para lo mismo. Conviene normalizarlo junto con la carga de stock.
- [ ] **Fase 5.6 — Carga de stock por modelo + variantes** (pendiente, es la parte que quedó del mismo pedido). Hoy `admin.html` pide un formulario completo (prenda, categoría, color, talle, ambos precios) **por cada unidad física**: un modelo en 3 colores × 4 talles son 12 cargas a mano repitiendo los mismos datos. La idea es una cabecera de modelo (prenda/categoría/precios) + una matriz color × talle × cantidad que se expanda del lado del cliente a N filas antes de postear. **`POST /admin/stock/lotes` no cambia** (sigue insertando 1 fila por unidad, que es correcto y coincide con el Java); es un cambio de UI. Incluir acá la normalización de talles.
- [ ] **Fase 6 — Retirar módulos del desktop ya migrados** (uno por uno, con paridad confirmada).

## Estado actual

Fase 5.5 completada (2026-08-07): tarjeta por producto + varias fotos por color, con la galería del detalle cambiando según el color elegido. No hay env vars nuevas. **Pendiente de decisión del usuario:** correr `node scripts/migrar-productos-json.js --apply` para pasar las 45 entradas publicadas al formato agrupado (hasta que se corra, el catálogo ya muestra una tarjeta por producto pero las fotos no están separadas por color en los productos viejos). Próximo paso: Fase 5.6 (carga de stock por modelo + variantes, con normalización de talles), y después Fase 6.

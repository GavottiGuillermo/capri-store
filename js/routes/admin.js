const express = require('express');

const router = express.Router();

// Scaffold del panel admin (Fase 0). Las rutas de auth y gestión
// se agregan en las fases siguientes del plan de migración (ver CLAUDE.md).

module.exports = router;
